#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
const int N = 100010;

/* ============================================ */

void solve() {
    int n;
    cin >> n;
}

/* ============================================ */

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;
    while (t--) solve();
}